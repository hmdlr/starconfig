// theme.js
import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    colors: {
        primary: "#0075F2",
        secondary: "#272727",
        tertiary: "#FED766",
    },
});

export default theme;
